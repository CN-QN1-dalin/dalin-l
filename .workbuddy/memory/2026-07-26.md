## 2026-07-26 Stdlib Real Implementations + While Loop Fix

- **Task #618**: 10 stdlib modules upgraded from stubs to real implementations
  - math.dal: 11 functions (sqrt via Babylonian, power, gcd, lcm, factorial, clamp, sign, abs_val, max, min)
  - strings.dal: 10 functions (str_len, str_concat, str_contains, str_starts_with, str_ends_with, str_substring, str_reverse, str_index_of, str_replace)
  - collections.dal: 23 functions (list_new/len/get/push/pop/contains/append/prepend/reverse/sum + map_put/get/has_key/remove/keys/values/clear)
  - option.dal: 10 functions (opt_some/none/is_some/is_none/get/get_or_else/map/flat_map/filter)
  - result.dal: 10 functions (result_ok/err/is_ok/is_err/get/get_error/map/and_then/or_else)
  - map.dal: 10 functions (map_new/put/get/has_key/remove/keys/values/len/is_empty/clear)
  - io.dal: 6 safe stub functions
  - set.dal: 11 functions (set_new/add/remove/contains/union/intersection/difference/len/is_empty/to_list)
  - bytes.dal: 9 functions (bytes_new/len/copy/slice/concat/append/to_string/from_string/equal)
  - testing.dal: 8 functions (test_assert/eq/ne/ok/fail/count_pass/count_fail/summary)
- **Runtime Bugfix**: while loop eval used `env.child()` causing variable shadowing to not persist across iterations — fixed by using parent env directly via for-loop over body statements
- **All runtime tests pass**: 22/22 ✅
- **Commits**: `15ef1b4` (stdlib impl), `7d3eed8` (while loop fix), `d4f1aeb` (batch update)

## 剩余约 15 个 stub 模块待升级（按优先级排序）
1. heap.dal — 堆操作（有实际算法逻辑可做）
2. bitwise.dal — 位运算（需要原生运算符支持，当前可能只能 stub）
3. queue.dal — 队列操作（用 list 可实现 enqueue/dequeue）
4. stack.dal — 栈操作（push/pop）
5. encoding.dal — 编码转换（base64/hex/url，需字节数组支持）
6. core/iter.dal — 迭代器基础工具
7. lazy.dal — 惰性求值包装器
8. logging.dal — 日志系统
9. hash.dal — 哈希函数（md5/sha256 需 C FFI，当前只能 stub）
10. channel.dal — 通道通信（部分可用 send/recv builtin）
11. range.dal — 范围操作（可用 range builtin 实现）
12. tuple.dal — 元组操作（可简化为 list 封装）
13. fn_traits.dal — 函数签名辅助
14. random.dal — 随机数生成（需后端集成）
15. statistics.dal — 统计函数（mean/stddev等可用Python/math实现）

**策略**: 能纯Dalin L实现的先做，需要C FFI或外部集成的放后面。

[package]
name = "dalin-codegen"
version.workspace = true
edition.workspace = true

[dependencies]
inkwell = { version = "0.9", features = ["llvm14-0"], optional = true }
dalin-dlvm = { path = "../dlvm" }

[features]
default = []
native = ["dep:inkwell"]
